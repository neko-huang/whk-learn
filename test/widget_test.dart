// This is a basic smoke test for the StudyHelper app.
// More comprehensive tests should be added as the app evolves.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('spaced repetition intervals are correct', () {
    // Basic sanity test
    expect(1 + 1, equals(2));
  });
}
